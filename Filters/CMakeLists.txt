### Find VTK
find_package(VTK REQUIRED)
include(${VTK_USE_FILE})

FILE(GLOB SRC_VTKSDIS_FILTERS *.cxx *.h)

ADD_LIBRARY( vtkSDIS SHARED ${SRC_VTKSDIS_FILTERS} )

TARGET_LINK_LIBRARIES( vtkSDIS ${VTK_LIBRARIES} )
